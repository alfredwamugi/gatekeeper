--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu

cairosvg>=2.7.1
openvino>=2023.3.0
opencv-python-headless>=4.8.0
ultralytics>=8.0.0
supervision>=0.18.0
psutil>=7.2.2
africastalking>=1.2.5

# Optional OCR and LPR dependencies live in requirements-ocr.txt.
# Note: model weight files (e.g., yolo11n.pt) are not Python packages - place them in the repo or set `model_path` to your weight location.
